# Разработка ApiSutra

Общая точка входа для человека и ИИ-агента, которые изменяют сам пакет
`brahmic/apisutra`: ядро, тесты, зависимости, документацию или CI.
Для разработки SDK внешнего API используйте [пользовательский маршрут](../start/create-sdk.md).
Подключение публичных casts, hooks и extensions также относится к использованию пакета.

## Начать работу

1. Прочитайте [AGENTS.md](../../AGENTS.md) и выберите применимые инструкции
   в [указателе правил](../../.agents/README.md). Для человека и ИИ действуют общие
   контракты, порядок реализации и проверки.
2. Изучите [архитектуру](architecture.md) и документ нужного механизма из таблицы ниже.
3. Сверьте ожидаемое поведение с [публичным справочником](../reference/README.md),
   текущими исходниками и тестами. Уточните границы задачи и совместимость.

При передаче задачи агенту укажите желаемый результат, наблюдаемый сценарий,
ограничения и способ проверки. Этот маршрут служит его входом в разработку ядра;
пользовательская [карточка задания](../start/agent.md) предназначена для задач SDK.

## Подготовить checkout

Нужны Git, PHP 8.4+ и Composer. Из корня репозитория:

```bash
composer install
composer test
```

Обычные тесты используют локальные зависимости, им не нужны внешнее приложение
и база данных. Проверки документации требуют Python 3.9+; Laravel и Redis имеют
отдельные стенды. Команды и условия — в [тестировании](testing.md).

## Выбрать область изменения

| Задача | Прочитать |
| --- | --- |
| Разобраться в связях модулей, клиентах и расширениях | [Архитектура](architecture.md) |
| Изменить стадии отдельного запроса | [Pipeline](pipeline.md) |
| Изменить пагинацию, composite, batch/pool или continuation | [Потоки выполнения](execution.md) |
| Изменить классификацию и доставку ошибок | [Ошибки](error-handling.md) |
| Изменить кеш, retry или квоты | [Связи механизмов](caching-retry.md) |
| Изменить чтение атрибутов и кеш метаданных | [Устройство атрибутов](attributes.md) |
| Добавить проверку, стенд или изменить CI | [Тестирование](testing.md) |
| Изменить документацию, пример или состав поставки | [Сопровождение документации](documentation.md) |

Эти документы объясняют устройство ядра. Полные пользовательские гарантии
принадлежат [справочнику](../reference/README.md); перечисление внутреннего
помощника здесь не делает его публичной точкой расширения.

## Выполнить изменение

1. Зафиксируйте наблюдаемое и ожидаемое поведение. Общий механизм остаётся
   в ApiSutra, особенности конкретного API — в SDK провайдера.
2. Измените затронутый механизм, сохраняя его связи и публичные контракты.
   При изменении поведения проверьте соответствующие сценарии, включая ошибки.
3. Обновите документ, которому принадлежит изменённый контракт, и зависимые примеры.
   При изменении совместимости опишите действия в [миграции](../migration/README.md).
4. Выполните применимые [проверки](testing.md). Укажите результат, ограничения
   проверки и последствия для пользователя в описании изменения.

Планы, аудиты и ADR ведутся по [workflow](../../.workflow/README.md).
Требования к описанию вклада — в [CONTRIBUTING](../../CONTRIBUTING.md).

## Поставка документации

Пользовательские README, руководства, справочник и примеры входят в Git/Composer
архивы. `docs/development/`, `CONTRIBUTING.md`, `.agents/`, `.workflow/` и тестовый
стенд доступны в checkout и исключены из архивов. Поэтому из пользовательских
страниц ссылки на разработку ведут в репозиторий по полному URL.

`composer check-package` проверяет состав обоих архивов, установку без dev-зависимостей
и standalone smoke. Для изменений рабочего дерева порядок проверки описан
в [тестировании](testing.md#проверки-самого-пакета).
